apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask
  namespace: pra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
        - name: flask
          image: pra/flask-sqlite:1.1 # <--- Change 1.0 en 1.1 ici !
          imagePullPolicy: IfNotPresent
          env:
            - name: DB_PATH
              value: /data/app.db
          ports:
            - containerPort: 8080
          command: ["/bin/sh","-lc"]
          args:
            - |
              cd /opt/app && gunicorn -w 2 -b 0.0.0.0:8080 app:app
          volumeMounts:
            - name: data
              mountPath: /data
            - name: backup         # <--- AJOUT : Nom du volume de backup
              mountPath: /backup   # <--- AJOUT : Chemin où Flask va chercher
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pra-data
        - name: backup         # <--- AJOUT : Déclaration du volume
          persistentVolumeClaim:
            claimName: pra-backup # <--- AJOUT : Nom du PVC existant